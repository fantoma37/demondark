# Demon Dark

- Use the existing Vite, React, TypeScript, Express, and Socket.IO stack.
- Keep gameplay state server-owned so all connected phones receive consistent phases and timers.
- Preserve the mobile-first visual language and avoid exposing another player's role to the wrong client.
- Run `npm run build` after changes.
